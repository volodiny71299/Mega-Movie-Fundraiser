# start of loop

# initial loop so that it runs at least once
name = ""
count = 1
MAX_TICKETS = 5

while name != "xxx" and count <= MAX_TICKETS:

    # get details
    name = input("Name: ")
    count += 1
